/**
 * Role Switcher Component
 * REMOVED: Role switching is no longer supported. Users should only have one role and use role-specific login pages.
 */

// This component is deprecated and should not be used
// Users should log in through role-specific login pages instead
const RoleSwitcher = () => {
  return null;
};

export default RoleSwitcher;
